import javax.swing.*;
import java.awt.*;

// for logic and iterating.
public class Trials {

    public static void main(String[] args) {

        TrialFrame t = new TrialFrame();

    /*    JFrame frame = new JFrame();
        frame.setTitle("THIS IS A GUI FOR TRIAL GAME"); // title
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // actually closes the window
        frame.setSize(1800, 1000); // size of GUI
        frame.setVisible(true); // make it so you can see gui
        frame.setResizable(false); // not resizable anymore

        //ImageIcon icon = new ImageIcon(); create image icon
        // frame.setIconImage(icon.getImage);

        frame.getContentPane().setBackground(new Color(0, 0, 0));
     */
    }
}
